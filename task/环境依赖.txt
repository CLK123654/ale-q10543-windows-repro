操作系统： Windows11、macOS或主流64位Linux。

主软件为Node.js20或更高兼容版本，使用ESmodules和标准库完成OpenAPI解析、schema差分、UTC日期计算、CSV处理、SDK关联与报告写出。无需安装第三方代码依赖，业务处理期间不联网。

执行账号需要读取压缩包根目录中的OpenAPI、策略、SDK清单和starter，并能在output目录写入脚本和报告。当前输入规模适合普通64位CPU与常规办公内存，不使用GPU；磁盘需要容纳输入、脚本和五份报告。需设置有限超时，覆盖本次发布复核并避免异常无限等待。
